sudo: required

language: bash

services:
  - docker

before_install:
   - docker build -t madsequipo14/mads-todolist-2017:0.2 .

script:
   - docker run --rm madsequipo14/mads-todolist-2017:0.2 /bin/bash -c "sbt test"
